import 'package:flutter/material.dart';

class EsignPage extends StatelessWidget {
  const EsignPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('hi this is esign page'),
      ),
    );
  }
}
