import 'package:flutter/material.dart';

class ArShowPage extends StatelessWidget {
  const ArShowPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('hi this is ar page'),
      ),
    );
  }
}
